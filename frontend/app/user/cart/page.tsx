import CartClient from "@/components/user/cart/CartClient";

export const metadata = {
  title: "Your Cart | ServiceApp",
  description: "Review and book your selected home services.",
};

export default function CartPage() {
  return <CartClient />;
}
