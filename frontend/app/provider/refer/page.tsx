import React from 'react';
import ProviderReferPage from '@/components/provider/refer/ProviderReferPage';

export const metadata = {
  title: 'Refer & Earn | Provider Partner Portal | BharatClap',
  description: 'Refer qualified service technicians to BharatClap and earn reward points on their first completed job.',
};

export default function ReferPage() {
  return <ProviderReferPage />;
}
