{
  "name": "serviceapp-micro-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix api-gateway && npm install --prefix auth-service && npm install --prefix booking-service && npm install --prefix catalog-service && npm install --prefix notification-service && npm install --prefix payment-service && npm install --prefix provider-service && npm install --prefix refund-service && npm install --prefix frontend",
    "predev:services": "node scripts/free-ports.js",
    "dev:services": "concurrently \"npm run dev --prefix api-gateway\" \"npm run dev --prefix auth-service\" \"npm run dev --prefix booking-service\" \"npm run dev --prefix catalog-service\" \"npm run dev --prefix notification-service\" \"npm run dev --prefix payment-service\" \"npm run dev --prefix provider-service\"",
    "dev:all": "concurrently \"npm run dev:services\" \"npm run dev --prefix frontend\"",
    "docker:infra": "docker-compose up -d mongodb redis"
  },
  "dependencies": {
    "@types/node": "^26.1.2",
    "concurrently": "^8.2.2",
    "express": "^4.22.2",
    "mongodb": "^7.2.0",
    "mongoose": "^8.24.2",
    "redis": "^6.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.1",
    "@types/express": "^4.17.25",
    "postcss": "^8.5.15",
    "tailwindcss": "^4.3.1"
  }
}
